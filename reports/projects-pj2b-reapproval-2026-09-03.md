# Pj2-B 再承認依頼: 「現状値不一致」で保留した 24 行 — 2026-09-03（CC）

**commit**: `f6375b9`（実行分）／本ファイルは追補
**前便**: `reports/projects-backfill-2026-09.md`（Pj2-A）・承認表 `02_計画・運営/検証記録_Pj2A_承認表_2026-09-03_ユウ.md`

---

## 0. なぜ保留したか（原因は Pj2-A の私の設計ミス）

Pj2-A の調査便で、Workflow に渡した `TARGETS` 配列が **#16 以降の `sourceUrl` を `''`（空文字）で申告し、`city` を「（所在地欠落）」と申告していた**。
調査エージェントはその申告を正として `current: "（なし）"` と記録し、そのまま承認表の「現状値」列になった。

- 実データには**当時から値が入っていた**（Pj2-A スナップショット `_pj2_targets.json` で確認済み）。
- つまり承認は「**空欄を埋める**」前提だったが、実行すると「**既存値を上書きする**」ことになる。
- Pj2-B の実行スクリプトは PATCH 前に現状値を GET 照合する設計（承認表と食い違えばスキップ）だったため、**24 行が自動的に止まった**。この安全弁は意図どおり働いた。

**恒久策（次便から適用）**: 調査エージェントには「欠落している」という**判断済みの申告を渡さず、対象レコードの既存全フィールドを生値で渡す**。
「何が欠けているか」はエージェント側が現物を見て判断する。

> 前回報告で「保留 21 行」と書きましたが、実行スクリプトの再走（dry-run・読取専用）で確定した正しい数は **24 行**です。
> 内訳の正: 実行 17 ／ スキップ 38 ＝ **現状値不一致 24 ＋ 既に提案値（冪等）11 ＋ 実測分岐 2 ＋ 却下 1**。

---

## A. データ破損の是正（3行）— ★優先。承認いただければ即実行

PR タイトルの断片が `city` に流入している。3 件とも**末尾が「市」**で切れており、取り込み時に「…市」で切り出した痕跡。
一覧・詳細の「所在地」に本文断片がそのまま出ている状態。

| # | slug | field | 現在の実データ | 提案値 |
|---|---|---|---|---|
| 20 | `pr-tecra-miyagi` | city | `【想定年利12.0%】不動産クラウドファンディング「TECROWD」、国内開発型ファンド「宮城県角田市` | `角田市` |
| 23 | `pr-co176308-bess` | city | `Solutions、東京電力グループと共同開発。日本の市` | `庄原市` |
| 40 | `pr-looop-saitama` | city | `～電力卸市` | `比企郡小川町` |

---

## B. 都道府県が誤っている（2行）— ★A と同じ取り込み事故の一部

「空欄を埋める」ではなく「**誤った既存値の訂正**」。A の 2 件と同一レコード。

| # | slug | field | 現在の実データ | 提案値 | 一次 |
|---|---|---|---|---|---|
| 7 | `pr-co160356-bess-2` | prefecture | `三重県` | `熊本県` | 熊本県益城町（ADW 益城） |
| 22 | `pr-co176308-bess` | prefecture | `東京都` | `広島県` | 広島県庄原市 |

---

## C. city が null（2行）— 承認の前提（空欄を埋める）と実態が一致

保留したのは表の現状値表記が「（市欠落）」で prefecture と混在しており、機械照合を通せなかったため。実データは `null` で、埋める操作として正しい。

| # | slug | field | 現在の実データ | 提案値 |
|---|---|---|---|---|
| 8 | `pr-co160356-bess-2` | city | `null` | `益城町` |
| 14 | `pr-co143072-bess` | city | `null` | `菊川市` |

---

## D. city の様式（郡を付けるか外すか）（2行）— ★2行が互いに逆向き

| # | slug | 現在の実データ | 提案値（正規化後） | 向き |
|---|---|---|---|---|
| 31 | `pr-co173175-shiga-4mwh` | `愛知郡愛荘町` | `愛荘町` | 郡を**外す** |
| 39 | `naganuma-bess` | `長沼町` | `夕張郡長沼町` | 郡を**付ける** |

実査（既存 city 値あり 215 件）: **都道府県を含まないのが 212 件で多数派**（付帯指示 (i) の根拠）。
一方「郡」の有無は既存データに両様式が混在しており、多数派が立たない。
→ **どちらかに寄せる規則をユウに決めていただきたい**（決めれば #31/#39 は自動的に決まる）。私からの推奨は「郡を含めない（`愛荘町`・`長沼町`）」——町村名だけで一意に定まり、既存の市表記（`角田市`・`庄原市`）と粒度が揃うため。

---

## E. cod の訂正（2行）— 既存値がリリース発表日で運開日ではない疑い

| # | slug | 現在の実データ | 提案値（正規化後） | 根拠 |
|---|---|---|---|---|
| 11 | `pr-co160356-bess-2` | `2026-02-12` | `2026-08-31` | 一次「2026年8月31日 竣工・引渡し、稼働開始」。既存 2/12 はリリース発表日と一致 |
| 42 | `pr-looop-saitama` | `2025-04-03` | `2025-02-21` | 一次「2025年2月21日」。既存 4/3 はリリース URL の日付（`4923_20250403`）と一致 |

---

## F. sourceUrl の差し替え（13行）

### F-0. 実質同一 URL（1行）— 判断不要・実行しない

| # | slug | 内容 |
|---|---|---|
| 13 | `pr-co164583-bess` | パーセントエンコード差のみ。デコードすると完全一致 → **差し替え不要** |

### F-1. 明確な改善（4行）— 企業トップ／一覧ページ → 個別リリース

| # | slug | 現在の実データ | 提案値 |
|---|---|---|---|
| 21 | `pr-tecra-miyagi` | `https://tecrowd.jp/`（トップ） | PR TIMES 000000143.000061009 |
| 43 | `pr-looop-saitama` | `https://looop.co.jp/`（トップ） | `https://looop.co.jp/info/4923_20250403` |
| 49 | `pr-co140317-bess` | `https://www.jicn.co.jp/`（トップ） | PR TIMES 000000034.000140317 |
| 50 | `jpn-gifu-sendai` | `https://www.nipponchikudenchi.co.jp/news/`（一覧） | PR TIMES 000000056.000161802 |

### F-2. 個別 → 個別の差し替え（8行）— ★積極的な理由が要る

現在の値も**個別記事の URL として機能している**。差し替えると「事業者の自社発表」から「PR TIMES 配信」へ、あるいは別リリースへ寄せることになる。

| # | slug | 現在の実データ | 提案値 | 所見 |
|---|---|---|---|---|
| 1 | `pr-co12501-bess` | `eco-st.co.jp/31935/`（自社個別） | PR TIMES 000000380.000012501 | 自社 → PR TIMES。現状で足りている |
| 2 | `pr-co76147-bess-2` | 適時開示PDF（`ssl4.eir-parts.net`） | PR TIMES 000000212.000076147 | PDF は消えやすい。差し替えに一定の合理性 |
| 5 | `pr-co55631-tokyo-4mw` | PR TIMES **000000104** | PR TIMES **000000129** | 同社の別リリース。どちらが当該案件かの判定が要る |
| 12 | `pr-co160356-bess-2` | PR TIMES **000000043** | PR TIMES **000000085** | 同上。#7/#8/#11 と同一レコード＝**熊本の一次に揃えるなら差し替えが筋** |
| 32 | `pr-co173175-shiga-4mwh` | `japan-energystorage.co.jp`（自社個別） | PR TIMES 000000001.000173175 | 自社 → PR TIMES。現状で足りている |
| 33 | `ota-bess` | PR TIMES **000000039** | PR TIMES **000000027** | 同社の別リリース。判定が要る |
| 37 | `naganuma-bess` | `jys-joyoshoji.co.jp/info/179/`（自社個別） | PR TIMES 000000121.000055631 | 自社 → PR TIMES。現状で足りている |
| 51 | `namie-redox-flow` | 日経BP 記事（`project.nikkeibp.co.jp`） | PR TIMES 000000007.000110789 | 二次（媒体記事）→ 一次（リリース）。**一次優先なら差し替えが筋** |

---

## 集計

| 区分 | 行数 | 私の推奨 |
|---|---|---|
| A データ破損の是正 | 3 | **実行**（優先） |
| B 都道府県の訂正 | 2 | **実行**（A と同一事故） |
| C city の追加（null → 値） | 2 | **実行** |
| D city の郡様式 | 2 | 規則の裁定後に実行 |
| E cod の訂正 | 2 | **実行**（既存値は発表日） |
| F-0 実質同一 URL | 1 | 実行しない |
| F-1 トップ／一覧 → 個別 | 4 | **実行** |
| F-2 個別 → 個別 | 8 | 個別裁定（#12・#51 は実行推奨、#2 は条件つき、残り5行は現状維持で足りる） |
| **計** | **24** | |

再承認をいただければ、同じスクリプト（現状値照合つき・PATCH 後 #106 全field照合）に承認済みの実データ値を入れて実行します。

---

*CC、2026-09-03。承認表の「現状値」列が誤っていたのは私の調査便設計の不備です。安全弁が働いて上書きは 1 件も起きていません。*
